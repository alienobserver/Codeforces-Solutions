#include <bits/stdc++.h>
using namespace std;

void solve(string n){
    cout << n.size() << endl;
}

int main()
{
    ios_base::sync_with_stdio(false);
    int t;
    string x;
    cin >> t;
    while (t--)
    {
        cin >> x;
        solve(x);
    }
    return 0;
}